const upload = require('./upload')
const uploadList = require('./component/uploadlist')
const uploadFile = require('./component/upload')

module.exports = {
  upload: upload,
  uploadList: uploadList,
  uploadFile: uploadFile
}